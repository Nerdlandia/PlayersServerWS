We recommend the node.js connect to the database using the Postgres user.  While this is not a best practice for security reasons, it facilitate getting the
application up and run as quickly as possible.  Note that the database and the table schema use the Postgres user as the OWNER.
